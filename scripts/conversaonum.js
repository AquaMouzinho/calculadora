function conversao(numero, base_inicial, base_final) {
  let resultado = '';
  if (base_inicial == base_final) {
    resultado = numero.toString();
  } else {
    switch (base_inicial) {
      case 'binario':

        break;
      case 'decimal':
        break;

      default:
        break;
    }
  }

  return resultado;
}

var btnConversao = document.getElementById('btnConvSis');
